(() => {
  if (window.StillProfessionalWorkspaceV188) return;
  const t = (en, hr) => document.documentElement.lang?.toLowerCase().startsWith('hr') ? hr : en;
  const api = async (path, options = {}) => { const response = await fetch(path, { credentials:'same-origin', headers:{'content-type':'application/json',...(options.headers||{})}, ...options }); const data = await response.json().catch(()=>({})); if (!response.ok) throw new Error(data.error || data.message || `HTTP ${response.status}`); return data; };
  async function openAvailability() {
    const data = await api('/api/v1/professional/profile'), p = data.profile || {};
    const dialog = document.createElement('dialog'); dialog.className='spn188-availability-dialog';
    dialog.innerHTML=`<form method="dialog" class="spn188-card"><button class="spn188-close" value="cancel" aria-label="${t('Close','Zatvori')}">×</button><span>${t('AVAILABILITY','DOSTUPNOST')}</span><h2>${t('When can people count on you?','Kada ljudi mogu računati na tebe?')}</h2><p>${t('Change availability without reopening the complete Professional Mode setup.','Promijeni dostupnost bez ponovnog otvaranja cijelog Profesionalnog načina.')}</p><label>${t('Status','Status')}<select name="availabilityStatus"><option value="AVAILABLE">Available</option><option value="LIMITED">Limited</option><option value="UNAVAILABLE">Unavailable</option><option value="PAUSED">Paused</option></select></label><label>${t('Weekly working capacity','Tjedni radni kapacitet')}<div class="spn188-hours"><input name="weeklyCapacityHours" type="number" min="1" max="168" placeholder="40"><b>${t('hours / week','sati / tjedno')}</b></div></label><fieldset><legend>${t('Where you can work','Gdje možeš raditi')}</legend><label><input type="checkbox" name="locationModes" value="REMOTE"> Remote</label><label><input type="checkbox" name="locationModes" value="LOCAL"> Local</label><label><input type="checkbox" name="locationModes" value="HYBRID"> Hybrid</label></fieldset><button class="primary spn188-save" value="save">${t('Save availability','Spremi dostupnost')}</button><output></output></form>`;
    document.body.append(dialog); dialog.querySelector('[name=availabilityStatus]').value=p.availabilityStatus||'AVAILABLE'; dialog.querySelector('[name=weeklyCapacityHours]').value=p.weeklyCapacityHours||''; (p.locationModes||[]).forEach(v=>{const el=dialog.querySelector(`[name=locationModes][value="${v}"]`);if(el)el.checked=true});
    dialog.addEventListener('close',()=>dialog.remove()); dialog.querySelector('.spn188-card').addEventListener('submit',async e=>{ if(e.submitter?.value!=='save') return; e.preventDefault(); const f=e.currentTarget, button=e.submitter; button.disabled=true; try { await api('/api/v1/professional/profile',{method:'PATCH',body:JSON.stringify({availabilityStatus:f.elements.availabilityStatus.value,weeklyCapacityHours:f.elements.weeklyCapacityHours.value?Number(f.elements.weeklyCapacityHours.value):null,locationModes:[...f.querySelectorAll('[name=locationModes]:checked')].map(x=>x.value)})}); dialog.close(); location.reload(); } catch(err){ f.querySelector('output').textContent=err.message; button.disabled=false; }}); dialog.showModal();
  }
  function bind(){ document.querySelectorAll('.spn136-availability button').forEach(button=>{ if(button.dataset.spn188)return; button.dataset.spn188='1'; button.replaceWith(button.cloneNode(true)); const fresh=document.querySelector('.spn136-availability button:not([data-spn188])'); if(fresh){fresh.dataset.spn188='1';fresh.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();openAvailability()},{capture:true});}}); }
  const observer=new MutationObserver(bind); observer.observe(document.documentElement,{subtree:true,childList:true}); bind(); window.StillProfessionalWorkspaceV188={openAvailability};
})();
