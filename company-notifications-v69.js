(()=>{const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),hr=()=>$('#language')?.value==='hr',t=(a,b)=>hr()?b:a;let timer;async function api(p,opt={}){const r=await fetch(p,{credentials:'same-origin',...opt});if(!r.ok)throw Error(r.status);return r.json()}function time(x){try{return new Date(x).toLocaleString()}catch{return x}}async function load(){let d;try{d=await api('/api/v1/merchant/notifications')}catch{return}let host=$('#companyNotifyV69');if(!host){host=document.createElement('div');host.id='companyNotifyV69';host.className='still-notify-v69';const target=document.querySelector('.cp46-session')||document.querySelector('.company-topbar')||document.querySelector('header');target?.appendChild(host)}if(!host)return;host.innerHTML=`<button class="sn69-button" aria-label="${t('Notifications','Obavijesti')}">♢${d.unread?`<i class="sn69-badge">${Math.min(d.unread,99)}</i>`:''}</button><div class="sn69-panel" hidden><div class="sn69-head"><strong>${t('Company notifications','Obavijesti tvrtke')}</strong><span>${d.unread} ${t('unread','nepročitano')}</span></div>${d.notifications.length?d.notifications.map(n=>`<a class="sn69-item ${n.severity} ${n.read_at?'read':''}" data-id="${esc(n.id)}" href="${esc(n.href||'#')}"><i class="sn69-dot"></i><div><b>${esc(n.title)}</b>${n.body?`<span>${esc(n.body)}</span>`:''}<small>${time(n.created_at)}</small></div></a>`).join(''):`<div class="sn69-empty">${t('Nothing needs your attention.','Ništa ne zahtijeva vašu pažnju.')}</div>`}</div>`;const btn=host.querySelector('.sn69-button'),panel=host.querySelector('.sn69-panel');btn.onclick=e=>{e.stopPropagation();panel.hidden=!panel.hidden};host.querySelectorAll('.sn69-item').forEach(a=>a.addEventListener('click',()=>fetch(`/api/v1/merchant/notifications/${encodeURIComponent(a.dataset.id)}/read`,{method:'POST',credentials:'same-origin'})));document.addEventListener('click',e=>{if(!host.contains(e.target))panel.hidden=true},{once:true})}function start(){load();clearInterval(timer);timer=setInterval(load,30000)}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',()=>setTimeout(start,700)):setTimeout(start,700)})();