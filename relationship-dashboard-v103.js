(() => {
  const $=(selector,root=document)=>root.querySelector(selector);
  const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
  const isHr=()=>$('#language')?.value==='hr'||document.documentElement.lang==='hr';
  const t=(en,hr)=>isHr()?hr:en;
  let buyerRoot=null,companyRoot=null,buyerData=null,companyData=null;

  async function api(path,options={}){const response=await fetch(path,{credentials:'same-origin',...options,headers:{...(options.body&&typeof options.body==='string'?{'content-type':'application/json'}:{}),...(options.headers||{})}});const data=await response.json().catch(()=>({}));if(!response.ok)throw Object.assign(new Error(data.message||data.error||`HTTP ${response.status}`),{status:response.status,data});return data}
  function initials(name){return String(name||'?').trim().split(/\s+/).slice(0,2).map(part=>part[0]||'').join('').toUpperCase()||'?'}
  function portrait(url,name,className=''){return `<span class="rd103-portrait ${className}">${url?`<img src="${esc(url)}" alt="${esc(name||'')}">`:`<b>${esc(initials(name))}</b>`}</span>`}
  function metric(value,label){return `<div><b>${Number(value||0).toLocaleString(isHr()?'hr-HR':'en-GB')}</b><span>${label}</span></div>`}
  function date(value){if(!value)return'—';try{return new Intl.DateTimeFormat(isHr()?'hr-HR':'en-GB',{dateStyle:'medium'}).format(new Date(value))}catch{return value}}
  function status(host,message,type=''){const node=$('[data-rd103-message]',host);if(node){node.className=`rd103-message ${type}`;node.textContent=message}}
  function buyerTools(){return `<nav class="rd103-tool-links"><a href="#decisionLabV83"><b>${t('Plan a purchase','Planiraj kupnju')}</b><span>${t('Compare readiness before paying','Usporedi spremnost prije plaćanja')} →</span></a><a href="#passportCommerceV92"><b>${t('Discover','Otkrij')}</b><span>${t('Offers and verified quotes','Ponude i verificirane ponude')} →</span></a><a href="#buyerRewardsV76"><b>${t('Rewards','Nagrade')}</b><span>${t('Points, benefits and reputation','Bodovi, pogodnosti i reputacija')} →</span></a><a href="#lifecyclePlatformV95"><b>${t('After purchase','Nakon kupnje')}</b><span>${t('History, support and true cost','Povijest, podrška i stvarni trošak')} →</span></a></nav>`}
  function configureProfileMedia(host,data,kind){const upload=$('.rd103-upload',host);if(kind==='buyer'){const visibility=$('.rd103-identity span',host);if(visibility&&data.profile?.shareWithConnectedBusinesses===false)visibility.textContent=t('PRIVATE PROFILE','PRIVATNI PROFIL')}if(data.capabilities?.profileMediaUploads||!upload)return;if(kind==='company')upload.innerHTML=`${t('Public company logo URL','Javna URL adresa logotipa tvrtke')}<input name="logoUrl" type="url" inputmode="url" value="${esc(data.profile?.logoSourceUrl||'')}" placeholder="https://…/logo.png"><small>${t('Use a stable HTTPS image URL. Still? retrieves it through a privacy-safe same-origin route for passports and QR centres.','Upotrijebite stabilnu HTTPS adresu slike. Still? je dohvaća kroz privatnu rutu istog izvora za putovnice i središta QR koda.')}</small>`;else upload.innerHTML=`<span>${t('Profile picture','Slika profila')}</span><small>${t('Your secured Google profile picture is used. Custom file upload will appear when private media storage is enabled.','Upotrebljava se tvoja zaštićena Google profilna slika. Prijenos vlastite datoteke pojavit će se kada se uključi privatna pohrana medija.')}</small>`}

  function buyerShell(){return `<header class="rd103-head"><div><span>${t('MY DASHBOARD','MOJA NADZORNA PLOČA')}</span><h2>${t('People, companies and passports in one relationship view.','Ljudi, tvrtke i putovnice u jednom prikazu odnosa.')}</h2><p>${t('Your profile travels with your account. A connected business sees it only when you already share a passport or case.','Tvoj profil putuje s računom. Povezana tvrtka vidi ga samo kada već dijelite putovnicu ili slučaj.')}</p></div><a href="#ownershipHubV83">${t('Open my passports','Otvori moje putovnice')} →</a></header><div class="rd103-loading">${t('Loading your dashboard…','Učitavanje nadzorne ploče…')}</div>`}
  function companyShell(){return `<header class="rd103-head"><div><span>${t('COMPANY DASHBOARD','NADZORNA PLOČA TVRTKE')}</span><h2>${t('Your company identity and buyer relationships.','Identitet vaše tvrtke i odnosi s kupcima.')}</h2><p>${t('Your profile appears on connected passports. Buyer profiles are visible only where a real relationship already exists.','Vaš profil pojavljuje se na povezanim putovnicama. Profili kupaca vidljivi su samo gdje stvarni odnos već postoji.')}</p></div><a href="#companyControlCenterV101">${t('Open company tools','Otvori poslovne alate')} →</a></header><div class="rd103-loading">${t('Loading company dashboard…','Učitavanje nadzorne ploče tvrtke…')}</div>`}

  function buyerDashboard(data){const p=data.profile||{},m=data.metrics||{},companies=data.companies||[],passports=data.recentPassports||[];return `<header class="rd103-head"><div><span>${t('BUYER DASHBOARD','NADZORNA PLOČA KUPCA')}</span><h2>${t('Your identity, relationships and next actions.','Tvoj identitet, odnosi i sljedeće radnje.')}</h2><p>${t('One calm view across passports, company commitments, cases and rewards.','Jedan miran prikaz putovnica, obećanja tvrtki, slučajeva i nagrada.')}</p></div><a href="#ownershipHubV83">${t('Open passports','Otvori putovnice')} →</a></header><div class="rd103-profile-grid"><section class="rd103-profile-card"><div class="rd103-identity">${portrait(p.pictureUrl,p.displayName,'large')}<div><span>${t('VISIBLE TO CONNECTED BUSINESSES','VIDLJIVO POVEZANIM TVRTKAMA')}</span><h3>${esc(p.displayName)}</h3><p>${esc(p.email||'')}</p></div><button type="button" data-rd103-edit>${t('Edit profile','Uredi profil')}</button></div><p class="rd103-bio">${esc(p.bio||t('Add a short profile description so connected businesses know who they are helping.','Dodaj kratak opis profila kako bi povezane tvrtke znale kome pomažu.'))}</p><form class="rd103-profile-form" data-rd103-buyer-profile hidden><label>${t('Display name','Ime za prikaz')}<input name="displayName" required maxlength="180" value="${esc(p.displayName)}"></label><label>${t('Short profile description','Kratak opis profila')}<textarea name="bio" maxlength="600">${esc(p.bio||'')}</textarea></label><label class="rd103-share"><input type="checkbox" name="shareWithConnectedBusinesses" ${p.shareWithConnectedBusinesses!==false?'checked':''}>${t('Show my picture and profile to businesses connected through a passport or case.','Prikaži moju sliku i profil tvrtkama povezanim kroz putovnicu ili slučaj.')}</label><label class="rd103-upload">${t('Profile picture','Slika profila')}<input type="file" data-rd103-photo accept="image/png,image/jpeg,image/webp"><small>${t('Square photo recommended. It is compressed before upload.','Preporučena je kvadratna slika. Komprimira se prije prijenosa.')}</small></label><button>${t('Save profile','Spremi profil')}</button></form><div class="rd103-message" data-rd103-message role="status"></div></section><section class="rd103-metrics">${metric(m.passports,t('Passports','Putovnice'))}${metric(m.connectedPassports,t('Company connected','Povezano s tvrtkom'))}${metric(m.openCommitments,t('Open commitments','Otvorena obećanja'))}${metric(m.cases,t('Cases','Slučajevi'))}${metric(m.points,t('Reward points','Bodovi nagrada'))}${metric(m.reputation,t('Reputation','Reputacija'))}</section></div><div class="rd103-columns"><section class="rd103-card"><header><div><span>${t('CONNECTED COMPANIES','POVEZANE TVRTKE')}</span><h3>${t('Who shares your passport history','Tko dijeli povijest putovnice')}</h3></div><b>${companies.length}</b></header><div class="rd103-list">${companies.map(company=>`<article>${portrait(company.logo_url,company.display_name,'company')}<div><b>${esc(company.display_name)}</b><small>${company.status==='verified'?`✓ ${t('Verified business','Verificirana tvrtka')}`:t('Business profile','Profil tvrtke')} · ${Number(company.passport_count||0)} ${t('passports','putovnica')}</small><p>${esc(company.description||company.support_email||'')}</p></div>${company.website_url?`<a href="${esc(company.website_url)}" target="_blank" rel="noopener">${t('Website','Web')} ↗</a>`:''}</article>`).join('')||`<div class="rd103-empty">${t('Connect a company-issued passport to build this relationship view.','Poveži putovnicu koju je izdala tvrtka kako bi izgradio ovaj prikaz odnosa.')}</div>`}</div></section><section class="rd103-card"><header><div><span>${t('RECENT PASSPORTS','NEDAVNE PUTOVNICE')}</span><h3>${t('Shared records and current state','Zajednički zapisi i trenutačno stanje')}</h3></div><b>${passports.length}</b></header><div class="rd103-list compact">${passports.map(item=>`<article>${portrait(item.logo_url,item.company_display_name||item.business_name,'company')}<div><b>${esc(item.title)}</b><small>${esc(item.public_id)} · ${esc(item.kind)} · ${esc(item.status)}</small><p>${esc(item.company_display_name||item.business_name||t('Buyer-owned passport','Putovnica kupca'))} · ${date(item.updated_at)}</p></div><a href="#ownershipHubV83">${t('Open','Otvori')} →</a></article>`).join('')||`<div class="rd103-empty">${t('Your account-synced passports will appear here.','Ovdje će se pojaviti putovnice sinkronizirane s računom.')}</div>`}</div></section></div>`}

  function companyDashboard(data){const p=data.profile||{},m=data.metrics||{},relationships=data.relationships||[],passports=data.recentPassports||[];return `<header class="rd103-head"><div><span>${t('COMPANY DASHBOARD','NADZORNA PLOČA TVRTKE')}</span><h2>${t('One identity across every buyer relationship.','Jedan identitet kroz svaki odnos s kupcem.')}</h2><p>${t('Your logo and public profile identify the issuer. Connected buyer profiles help the team continue the relationship without asking for the same context again.','Vaš logotip i javni profil identificiraju izdavatelja. Profili povezanih kupaca pomažu timu nastaviti odnos bez ponovnog traženja istog konteksta.')}</p></div><a href="#companyControlCenterV101">${t('Open company tools','Otvori poslovne alate')} →</a></header><div class="rd103-profile-grid"><section class="rd103-profile-card"><div class="rd103-identity">${portrait(p.logoUrl,p.displayName,'large company')}<div><span>${p.verified?`✓ ${t('VERIFIED COMPANY PROFILE','VERIFICIRANI PROFIL TVRTKE')}`:t('COMPANY PROFILE','PROFIL TVRTKE')}</span><h3>${esc(p.displayName)}</h3><p>${esc(p.supportEmail||p.websiteUrl||'')}</p></div><button type="button" data-rd103-edit>${t('Edit profile','Uredi profil')}</button></div><p class="rd103-bio">${esc(p.description||t('Add a concise description buyers will recognize on passports and relationship cards.','Dodajte sažet opis koji će kupci prepoznati na putovnicama i karticama odnosa.'))}</p><form class="rd103-profile-form" data-rd103-company-profile hidden><label>${t('Public company name','Javni naziv tvrtke')}<input name="displayName" required maxlength="180" value="${esc(p.displayName)}"></label><label>${t('Description','Opis')}<textarea name="description" maxlength="900">${esc(p.description||'')}</textarea></label><div><label>${t('Website','Web-stranica')}<input name="websiteUrl" type="url" value="${esc(p.websiteUrl||'')}"></label><label>${t('Support email','E-mail podrške')}<input name="supportEmail" type="email" value="${esc(p.supportEmail||'')}"></label></div><label class="rd103-upload">${t('Company logo','Logotip tvrtke')}<input type="file" data-rd103-logo accept="image/png,image/jpeg,image/webp"><small>${t('Square transparent logo recommended. It will appear in passport QR codes.','Preporučen je kvadratni prozirni logotip. Pojavit će se u QR kodovima putovnica.')}</small></label><button>${t('Save company profile','Spremi profil tvrtke')}</button></form><div class="rd103-message" data-rd103-message role="status"></div></section><section class="rd103-metrics">${metric(m.buyers,t('Connected buyers','Povezani kupci'))}${metric(m.connectedPassports,t('Connected passports','Povezane putovnice'))}${metric(m.openCases,t('Open cases','Otvoreni slučajevi'))}${metric(m.activeServices,t('Active services','Aktivne usluge'))}${metric(m.openTasks,t('Open tasks','Otvoreni zadaci'))}${metric(m.openCommitments,t('Open commitments','Otvorena obećanja'))}</section></div><nav class="rd103-tool-links"><a href="#companyPassportStudioV83"><b>${t('Passports','Putovnice')}</b><span>${t('Issue and update shared records','Izdajte i ažurirajte zajedničke zapise')} →</span></a><a href="#companyOpsV74"><b>${t('Today','Danas')}</b><span>${t('Tasks, approvals and commitments','Zadaci, odobrenja i obećanja')} →</span></a><a href="#companyControlCenterV101"><b>${t('Advanced tools','Napredni alati')}</b><span>${t('Delivery, capacity and Customer 360','Izvršenje, kapacitet i Customer 360')} →</span></a><a href="#company-tools-preview"><b>${t('Demo mode','Demo način')}</b><span>${t('Practice all 29 modules safely','Sigurno isprobajte svih 29 modula')} →</span></a></nav><div class="rd103-columns"><section class="rd103-card"><header><div><span>${t('CONNECTED BUYER PROFILES','PROFILI POVEZANIH KUPACA')}</span><h3>${t('People your team already serves','Ljudi kojima vaš tim već pruža usluge')}</h3></div><b>${relationships.length}</b></header><div class="rd103-list">${relationships.map(person=>`<article>${portrait(person.picture_url,person.display_name)}<div><b>${esc(person.display_name)}</b><small>${Number(person.passport_count||0)} ${t('connected passports','povezanih putovnica')} · ${date(person.last_activity)}</small><p>${esc(person.bio||person.email||'')}</p></div><a href="#companyControlCenterV101">Customer 360 →</a></article>`).join('')||`<div class="rd103-empty">${t('Buyer profiles appear after they connect a passport or case and allow relationship sharing.','Profili kupaca pojavljuju se nakon povezivanja putovnice ili slučaja i dopuštenja dijeljenja odnosa.')}</div>`}</div></section><section class="rd103-card"><header><div><span>${t('RECENT SHARED PASSPORTS','NEDAVNE ZAJEDNIČKE PUTOVNICE')}</span><h3>${t('Buyer acceptance and current state','Prihvat kupca i trenutačno stanje')}</h3></div><b>${passports.length}</b></header><div class="rd103-list compact">${passports.map(item=>`<article>${portrait(item.buyer_picture,item.buyer_name)}<div><b>${esc(item.title)}</b><small>${esc(item.public_id)} · ${esc(item.kind)} · ${esc(item.status)}</small><p>${esc(item.buyer_name||t('Awaiting buyer connection','Čeka povezivanje kupca'))} · ${date(item.updated_at)}</p></div><a href="#companyPassportStudioV83">${t('Open','Otvori')} →</a></article>`).join('')||`<div class="rd103-empty">${t('Issue a passport to start a durable buyer relationship.','Izdajte putovnicu za početak trajnog odnosa s kupcem.')}</div>`}</div></section></div>`}

  async function imageBlob(file,size,cover=true){if(!file||!/^image\/(png|jpeg|webp)$/.test(file.type))throw new Error('invalid_image');const url=URL.createObjectURL(file);try{const image=await new Promise((resolve,reject)=>{const node=new Image();node.onload=()=>resolve(node);node.onerror=reject;node.src=url});const canvas=document.createElement('canvas');canvas.width=size;canvas.height=size;const context=canvas.getContext('2d');context.fillStyle='#ffffff';if(!file.type.includes('png'))context.fillRect(0,0,size,size);const scale=cover?Math.max(size/image.naturalWidth,size/image.naturalHeight):Math.min(size/image.naturalWidth,size/image.naturalHeight),width=image.naturalWidth*scale,height=image.naturalHeight*scale;context.drawImage(image,(size-width)/2,(size-height)/2,width,height);return await new Promise(resolve=>canvas.toBlob(resolve,'image/webp',.84))}finally{URL.revokeObjectURL(url)}}
  async function upload(file,kind,host){status(host,t('Preparing and uploading image…','Priprema i prijenos slike…'));const blob=await imageBlob(file,kind==='buyer'?320:420,kind==='buyer');if(!blob||blob.size>350000)throw new Error('image_too_large');const result=await api(kind==='buyer'?'/api/v1/buyer-profile/photo':'/api/v1/business-profile/logo',{method:'POST',body:blob,headers:{'content-type':'image/webp'}});status(host,t('Image saved.','Slika je spremljena.'),'success');return result}
  async function loadBuyer(){if(!buyerRoot)return;try{buyerData=await api('/api/v1/buyer-dashboard');buyerRoot.innerHTML=buyerDashboard(buyerData);$('.rd103-profile-grid',buyerRoot)?.insertAdjacentHTML('afterend',buyerTools());configureProfileMedia(buyerRoot,buyerData,'buyer');window.StillIdentityV103={...(window.StillIdentityV103||{}),buyerProfile:buyerData.profile,companyList:buyerData.companies||[],companies:Object.fromEntries((buyerData.companies||[]).map(company=>[company.organization_id,company]))};window.dispatchEvent(new CustomEvent('still:identity-ready',{detail:window.StillIdentityV103}))}catch(error){buyerRoot.innerHTML=buyerShell()+`<div class="rd103-error">${error.status===429?t('The account service is temporarily at capacity. Your buyer tools remain unchanged.','Usluga računa privremeno je na granici kapaciteta. Alati kupca ostaju nepromijenjeni.'):t('The relationship dashboard is temporarily unavailable.','Nadzorna ploča odnosa trenutačno nije dostupna.')}</div>`}}
  async function loadCompany(){if(!companyRoot)return;try{companyData=await api('/api/v1/business-dashboard');companyRoot.innerHTML=companyDashboard(companyData);configureProfileMedia(companyRoot,companyData,'company');window.StillIdentityV103={...(window.StillIdentityV103||{}),companyProfile:companyData.profile,relationships:companyData.relationships||[]};window.dispatchEvent(new CustomEvent('still:identity-ready',{detail:window.StillIdentityV103}))}catch(error){companyRoot.innerHTML=companyShell()+`<div class="rd103-error">${error.status===429?t('Cloud capacity is temporarily limiting live company data. The safe demo remains available.','Kapacitet oblaka privremeno ograničava stvarne podatke tvrtke. Siguran demo ostaje dostupan.'):t('The company relationship dashboard is temporarily unavailable.','Nadzorna ploča odnosa tvrtke trenutačno nije dostupna.')}</div>`}}
  function mountBuyer(detail){if(!detail?.authenticated||document.body.classList.contains('business-page'))return;const anchor=$('#ownershipPlatformV83 .op83-home');if(!anchor)return setTimeout(()=>mountBuyer(detail),300);buyerRoot=$('#relationshipDashboardV103')||document.createElement('section');buyerRoot.id='relationshipDashboardV103';buyerRoot.className='rd103 rd103-buyer';if(!buyerRoot.isConnected)anchor.insertAdjacentElement('afterend',buyerRoot);buyerRoot.innerHTML=buyerShell();loadBuyer()}
  function mountCompany(detail){if(!detail?.organization||!document.body.classList.contains('business-page'))return;const anchor=$('#companyPortalV46');if(!anchor)return setTimeout(()=>mountCompany(detail),300);companyRoot=$('#companyRelationshipDashboardV103')||document.createElement('section');companyRoot.id='companyRelationshipDashboardV103';companyRoot.className='rd103 rd103-company';if(!companyRoot.isConnected)anchor.insertAdjacentElement('afterend',companyRoot);companyRoot.innerHTML=companyShell();loadCompany()}

  document.addEventListener('click',event=>{const edit=event.target.closest('[data-rd103-edit]');if(edit){const host=edit.closest('.rd103'),form=$('.rd103-profile-form',host);form.hidden=!form.hidden;edit.textContent=form.hidden?t('Edit profile','Uredi profil'):t('Close editor','Zatvori uređivanje')}});
  document.addEventListener('change',async event=>{const input=event.target;if(!input.matches('[data-rd103-photo],[data-rd103-logo]')||!input.files?.[0])return;const host=input.closest('.rd103');input.disabled=true;try{await upload(input.files[0],input.matches('[data-rd103-photo]')?'buyer':'company',host);if(input.matches('[data-rd103-photo]'))await loadBuyer();else await loadCompany()}catch(error){status(host,error.message==='image_too_large'?t('The image is too large after compression. Choose a simpler image.','Slika je prevelika nakon kompresije. Odaberite jednostavniju sliku.'):t('The image could not be saved.','Sliku nije moguće spremiti.'),'error')}finally{input.disabled=false}});
  document.addEventListener('submit',async event=>{const buyer=event.target.closest('[data-rd103-buyer-profile]'),company=event.target.closest('[data-rd103-company-profile]');if(!buyer&&!company)return;event.preventDefault();const form=buyer||company,host=form.closest('.rd103'),data=Object.fromEntries(new FormData(form));if(buyer){data.shareWithConnectedBusinesses=form.shareWithConnectedBusinesses.checked;data.shareContactWithConnectedBusinesses=!!form.shareContactWithConnectedBusinesses?.checked}const button=$('button[type="submit"],button:not([type])',form);button.disabled=true;try{await api(buyer?'/api/v1/buyer-profile':'/api/v1/business-profile',{method:'POST',body:JSON.stringify(data)});status(host,t('Profile saved.','Profil je spremljen.'),'success');if(buyer)await loadBuyer();else await loadCompany()}catch{status(host,t('The profile could not be saved. Check the fields and retry.','Profil nije moguće spremiti. Provjerite polja i pokušajte ponovno.'),'error')}finally{button.disabled=false}});
  window.addEventListener('still:buyer-authenticated',event=>mountBuyer(event.detail));
  window.addEventListener('still:company-authenticated',event=>mountCompany(event.detail));
})();
