(()=>{const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),hr=()=>document.querySelector('#language')?.value==='hr',t=(a,b)=>hr()?b:a;function cases(){try{return JSON.parse(localStorage.getItem('still_case_access_v1')||'{}')}catch{return{}}}async function get(pid,token){const r=await fetch(`/api/v1/cases/${encodeURIComponent(pid)}/notifications`,{headers:{'x-still-case-token':token}});if(!r.ok)throw Error();return r.json()}async function mark(pid,token,id){fetch(`/api/v1/cases/${encodeURIComponent(pid)}/notifications/${encodeURIComponent(id)}/read`,{method:'POST',headers:{'x-still-case-token':token}}).catch(()=>{})}async function load(){const access=cases(),entries=Object.entries(access);if(!entries.length)return;const all=[];for(const [pid,v] of entries.slice(-20)){const token=typeof v==='string'?v:v?.token;if(!token)continue;try{const d=await get(pid,token);d.notifications.forEach(n=>all.push({...n,pid,token}))}catch{}}all.sort((a,b)=>String(b.created_at).localeCompare(String(a.created_at)));const unread=all.filter(x=>!x.read_at).length;let host=document.querySelector('#buyerNotifyV69');if(!host){host=document.createElement('div');host.id='buyerNotifyV69';host.className='still-notify-v69';(document.querySelector('.header-actions')||document.querySelector('.topbar')||document.querySelector('header'))?.appendChild(host)}if(!host)return;host.innerHTML=`<button class="sn69-button" aria-label="${t('Notifications','Obavijesti')}">♢${unread?`<i class="sn69-badge">${Math.min(unread,99)}</i>`:''}</button><div class="sn69-panel" hidden><div class="sn69-head"><strong>${t('Purchase updates','Novosti o kupnjama')}</strong><span>${unread} ${t('unread','nepročitano')}</span></div>${all.length?all.slice(0,50).map(n=>`<a class="sn69-item ${n.severity} ${n.read_at?'read':''}" data-id="${esc(n.id)}" data-pid="${esc(n.pid)}" href="${esc(n.href||'#')}"><i class="sn69-dot"></i><div><b>${esc(n.title)}</b>${n.body?`<span>${esc(n.body)}</span>`:''}<small>${esc(n.pid)} · ${new Date(n.created_at).toLocaleString()}</small></div></a>`).join(''):`<div class="sn69-empty">${t('No case updates yet.','Još nema novosti o slučajevima.')}</div>`}</div>`;const p=host.querySelector('.sn69-panel');host.querySelector('.sn69-button').onclick=e=>{e.stopPropagation();p.hidden=!p.hidden};host.querySelectorAll('.sn69-item').forEach(a=>a.onclick=()=>{const n=all.find(x=>x.id===a.dataset.id);if(n)mark(n.pid,n.token,n.id)})}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',()=>setTimeout(load,1200)):setTimeout(load,1200);setInterval(load,45000)})();