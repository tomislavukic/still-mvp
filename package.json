{
  "name": "still-mvp",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "syntax": "node scripts/check-syntax.js",
    "build": "node build-public.js",
    "test": "node smoke-test.js && node scripts/world-foundation-test.js && node scripts/still-os-test.js && node scripts/needs-resolution-test.js && node scripts/still-market-test.js",
    "test:world": "node scripts/world-foundation-test.js",
    "test:os": "node scripts/still-os-test.js",
    "test:needs": "node scripts/needs-resolution-test.js",
    "test:market": "node scripts/still-market-test.js",
    "test:world:integration": "node scripts/run-world-foundation-integration.js",
    "test:world:sqlite": "node scripts/run-worker-sqlite-integration.js",
    "security": "gitleaks git --config .gitleaks.toml --redact --no-banner --exit-code 1",
    "validate:codeql": "node scripts/validate-codeql-config.js",
    "validate:bundle": "node scripts/validate-production-bundle.js",
    "validate:app": "npm run syntax && npm run build && npm test && npm run test:world:integration",
    "validate": "npm run validate:app && npm run validate:codeql && npm run security",
    "validate:deploy": "npm run validate && npx --yes wrangler@4.118.0 deploy --dry-run --outdir .wrangler/validate-deploy",
    "check": "npm run syntax && npm test",
    "deploy": "npm run check && npm run build && npx --yes wrangler@4.118.0 deploy"
  }
}
