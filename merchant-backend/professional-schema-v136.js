export const PROFESSIONAL_SCHEMA_V136 = [
  `CREATE TABLE IF NOT EXISTS professional_profiles(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,buyer_account_id TEXT NOT NULL UNIQUE,professional_mode_enabled INTEGER NOT NULL DEFAULT 0,display_name TEXT NOT NULL,headline TEXT,bio TEXT,coarse_location TEXT,location_modes_json TEXT NOT NULL DEFAULT '[]',availability_status TEXT NOT NULL DEFAULT 'UNAVAILABLE',weekly_capacity_hours INTEGER,minimum_project_cents INTEGER,currency TEXT NOT NULL DEFAULT 'EUR',profile_status TEXT NOT NULL DEFAULT 'DRAFT',created_at TEXT NOT NULL,updated_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS professional_capabilities(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,profile_id TEXT NOT NULL,capability_key TEXT NOT NULL,label TEXT NOT NULL,category TEXT NOT NULL DEFAULT 'OTHER',level TEXT,description TEXT,years_experience INTEGER,source_type TEXT NOT NULL DEFAULT 'USER_DECLARED',verification_status TEXT NOT NULL DEFAULT 'UNVERIFIED',created_at TEXT NOT NULL,updated_at TEXT NOT NULL,UNIQUE(profile_id,capability_key))`,
  `CREATE TABLE IF NOT EXISTS professional_capability_evidence(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,capability_id TEXT NOT NULL,evidence_type TEXT NOT NULL,source_type TEXT NOT NULL,source_public_id TEXT,title TEXT NOT NULL,description TEXT,verified_at TEXT,created_at TEXT NOT NULL,UNIQUE(capability_id,evidence_type,source_type,source_public_id))`,
  `CREATE TABLE IF NOT EXISTS professional_portfolio_items(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,profile_id TEXT NOT NULL,title TEXT NOT NULL,description TEXT,external_url TEXT,document_id TEXT,capability_keys_json TEXT NOT NULL DEFAULT '[]',is_public INTEGER NOT NULL DEFAULT 1,created_at TEXT NOT NULL,updated_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS professional_briefs(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,need_id TEXT NOT NULL,buyer_account_id TEXT NOT NULL,title TEXT NOT NULL,objective TEXT NOT NULL,scope TEXT NOT NULL,deliverables_json TEXT NOT NULL DEFAULT '[]',requirements_json TEXT NOT NULL DEFAULT '[]',excluded_context_json TEXT NOT NULL DEFAULT '[]',budget_min_cents INTEGER,budget_max_cents INTEGER,currency TEXT NOT NULL DEFAULT 'EUR',deadline TEXT,location_mode TEXT,coarse_location TEXT,generation_method TEXT NOT NULL DEFAULT 'EXPLICIT',confirmed_at TEXT,matching_enabled INTEGER NOT NULL DEFAULT 0,created_at TEXT NOT NULL,updated_at TEXT NOT NULL,UNIQUE(buyer_account_id,need_id))`,
  `CREATE TABLE IF NOT EXISTS professional_brief_attachments(id TEXT PRIMARY KEY,brief_id TEXT NOT NULL,document_id TEXT NOT NULL,approved_at TEXT NOT NULL,created_at TEXT NOT NULL,UNIQUE(brief_id,document_id))`,
  `CREATE TABLE IF NOT EXISTS professional_opportunities(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,brief_id TEXT NOT NULL,client_buyer_account_id TEXT NOT NULL,professional_buyer_account_id TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'OPEN',match_score INTEGER NOT NULL,match_reasons_json TEXT NOT NULL DEFAULT '[]',failed_constraints_json TEXT NOT NULL DEFAULT '[]',shared_at TEXT NOT NULL,viewed_at TEXT,declined_at TEXT,closed_at TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL,UNIQUE(brief_id,professional_buyer_account_id))`,
  `CREATE TABLE IF NOT EXISTS professional_opportunity_messages(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,opportunity_id TEXT NOT NULL,author_buyer_account_id TEXT NOT NULL,body TEXT NOT NULL,created_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS professional_proposals(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,opportunity_id TEXT NOT NULL,professional_buyer_account_id TEXT NOT NULL,client_buyer_account_id TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'SENT',current_version INTEGER NOT NULL DEFAULT 1,amount_cents INTEGER NOT NULL,currency TEXT NOT NULL DEFAULT 'EUR',summary TEXT NOT NULL,scope TEXT NOT NULL,estimated_days INTEGER,deadline TEXT,revision_rounds INTEGER NOT NULL DEFAULT 1,milestones_json TEXT NOT NULL DEFAULT '[]',expires_at TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL,accepted_at TEXT,declined_at TEXT,withdrawn_at TEXT)`,
  `CREATE TABLE IF NOT EXISTS professional_proposal_versions(id TEXT PRIMARY KEY,proposal_id TEXT NOT NULL,version_number INTEGER NOT NULL,author_buyer_account_id TEXT NOT NULL,amount_cents INTEGER NOT NULL,currency TEXT NOT NULL,summary TEXT NOT NULL,scope TEXT NOT NULL,estimated_days INTEGER,deadline TEXT,revision_rounds INTEGER NOT NULL,milestones_json TEXT NOT NULL DEFAULT '[]',change_note TEXT,created_at TEXT NOT NULL,UNIQUE(proposal_id,version_number))`,
  `CREATE TABLE IF NOT EXISTS professional_deals(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,proposal_id TEXT NOT NULL UNIQUE,client_buyer_account_id TEXT NOT NULL,professional_buyer_account_id TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'AGREED',amount_cents INTEGER NOT NULL,currency TEXT NOT NULL,payment_mode TEXT NOT NULL DEFAULT 'EXTERNAL_MANUAL',payment_status TEXT NOT NULL DEFAULT 'NOT_RECORDED',platform_fee_cents INTEGER NOT NULL DEFAULT 0,agreed_at TEXT NOT NULL,completed_at TEXT,cancelled_at TEXT,updated_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS professional_projects(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,deal_id TEXT NOT NULL UNIQUE,brief_id TEXT NOT NULL,need_id TEXT NOT NULL,client_buyer_account_id TEXT NOT NULL,professional_buyer_account_id TEXT NOT NULL,title TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'ACTIVE',started_at TEXT NOT NULL,due_at TEXT,completion_requested_at TEXT,completed_at TEXT,cancelled_at TEXT,updated_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS professional_milestones(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,project_id TEXT NOT NULL,title TEXT NOT NULL,description TEXT,amount_cents INTEGER,currency TEXT NOT NULL DEFAULT 'EUR',sequence_number INTEGER NOT NULL,status TEXT NOT NULL DEFAULT 'PENDING',due_at TEXT,submitted_at TEXT,approved_at TEXT,revision_requested_at TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL,UNIQUE(project_id,sequence_number))`,
  `CREATE TABLE IF NOT EXISTS professional_deliverables(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,project_id TEXT NOT NULL,milestone_id TEXT,author_buyer_account_id TEXT NOT NULL,title TEXT NOT NULL,description TEXT,document_id TEXT,external_url TEXT,status TEXT NOT NULL DEFAULT 'SUBMITTED',submitted_at TEXT NOT NULL,approved_at TEXT,revision_requested_at TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS professional_project_messages(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,project_id TEXT NOT NULL,author_buyer_account_id TEXT NOT NULL,body TEXT NOT NULL,created_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS professional_project_events(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,project_id TEXT NOT NULL,actor_buyer_account_id TEXT NOT NULL,event_type TEXT NOT NULL,title TEXT NOT NULL,details_json TEXT NOT NULL DEFAULT '{}',created_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS professional_feedback(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,project_id TEXT NOT NULL UNIQUE,client_buyer_account_id TEXT NOT NULL,professional_buyer_account_id TEXT NOT NULL,outcome_rating INTEGER NOT NULL,communication_rating INTEGER NOT NULL,reliability_rating INTEGER NOT NULL,comment TEXT,created_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS professional_favorites(client_buyer_account_id TEXT NOT NULL,professional_buyer_account_id TEXT NOT NULL,source_project_id TEXT NOT NULL,created_at TEXT NOT NULL,PRIMARY KEY(client_buyer_account_id,professional_buyer_account_id))`,
  `CREATE TABLE IF NOT EXISTS professional_reports(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,reporter_buyer_account_id TEXT NOT NULL,target_buyer_account_id TEXT,project_id TEXT,reason TEXT NOT NULL,details TEXT,status TEXT NOT NULL DEFAULT 'OPEN',created_at TEXT NOT NULL,updated_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS professional_disputes(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,project_id TEXT NOT NULL,opened_by_buyer_account_id TEXT NOT NULL,reason TEXT NOT NULL,details TEXT,status TEXT NOT NULL DEFAULT 'OPEN',created_at TEXT NOT NULL,updated_at TEXT NOT NULL,resolved_at TEXT)`,
  `CREATE TABLE IF NOT EXISTS professional_notifications(id TEXT PRIMARY KEY,public_id TEXT NOT NULL UNIQUE,buyer_account_id TEXT NOT NULL,source_key TEXT NOT NULL UNIQUE,notification_type TEXT NOT NULL,title TEXT NOT NULL,body TEXT,href TEXT NOT NULL,read_at TEXT,created_at TEXT NOT NULL)`,
  `CREATE TABLE IF NOT EXISTS professional_fee_config(id TEXT PRIMARY KEY CHECK(id='active'),basis_points INTEGER NOT NULL DEFAULT 0,fixed_fee_cents INTEGER NOT NULL DEFAULT 0,updated_at TEXT NOT NULL)`,
  `CREATE INDEX IF NOT EXISTS idx_professional_profiles_discovery ON professional_profiles(professional_mode_enabled,profile_status,availability_status,updated_at DESC)`,
  `CREATE INDEX IF NOT EXISTS idx_professional_capabilities_profile ON professional_capabilities(profile_id,capability_key)`,
  `CREATE INDEX IF NOT EXISTS idx_professional_opportunities_professional ON professional_opportunities(professional_buyer_account_id,status,updated_at DESC)`,
  `CREATE INDEX IF NOT EXISTS idx_professional_opportunities_client ON professional_opportunities(client_buyer_account_id,status,updated_at DESC)`,
  `CREATE INDEX IF NOT EXISTS idx_professional_opportunity_messages ON professional_opportunity_messages(opportunity_id,created_at)`,
  `CREATE INDEX IF NOT EXISTS idx_professional_proposals_participants ON professional_proposals(client_buyer_account_id,professional_buyer_account_id,status,updated_at DESC)`,
  `CREATE INDEX IF NOT EXISTS idx_professional_projects_participants ON professional_projects(client_buyer_account_id,professional_buyer_account_id,status,updated_at DESC)`,
  `CREATE INDEX IF NOT EXISTS idx_professional_milestones_project ON professional_milestones(project_id,sequence_number)`,
  `CREATE INDEX IF NOT EXISTS idx_professional_deliverables_project ON professional_deliverables(project_id,created_at)`,
  `CREATE INDEX IF NOT EXISTS idx_professional_notifications_owner ON professional_notifications(buyer_account_id,read_at,created_at DESC)`
];
